// Import validator for albums
const AlbumsValidator = require('./albums');

// Import validator for songs
const SongsValidator = require('./songs');

// Export both validators
module.exports = { AlbumsValidator, SongsValidator };
